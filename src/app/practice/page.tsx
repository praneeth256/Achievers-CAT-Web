"use client";

import { useEffect, useMemo, useState } from "react";
import Link from "next/link";
import { collection, getDocs, query, where } from "firebase/firestore";
import { onAuthStateChanged, type User } from "firebase/auth";
import { ArrowRight, BookOpenCheck, CheckCircle2, ChevronLeft, ChevronRight, Circle, Loader2, RotateCcw, XCircle } from "lucide-react";
import { auth, db } from "@/lib/firebase/client";

type Question = { id: string; section: "Quant" | "VARC" | "DILR"; chapter: string; difficulty: "Easy" | "Moderate" | "Hard"; question: string; options: string[]; correctOption: string; explanation?: string };
const styles = { Easy: "bg-brand-tint text-brand-darker", Moderate: "bg-amber-50 text-amber-700", Hard: "bg-red-50 text-red-600" };
export default function PracticePage() {
  const [user, setUser] = useState<User | null>(null), [questions, setQuestions] = useState<Question[]>([]), [loading, setLoading] = useState(true), [section, setSection] = useState("Quant"), [chapter, setChapter] = useState(""), [index, setIndex] = useState(0), [answers, setAnswers] = useState<Record<string, string>>({});
  useEffect(() => onAuthStateChanged(auth, setUser), []);
  useEffect(() => { getDocs(query(collection(db, "practice_questions"), where("published", "==", true))).then((snapshot) => setQuestions(snapshot.docs.map((item) => ({ id: item.id, ...item.data() }) as Question))).catch(console.error).finally(() => setLoading(false)); }, []);
  const chapters = useMemo(() => [...new Set(questions.filter((item) => item.section === section).map((item) => item.chapter))].sort(), [questions, section]);
  useEffect(() => { setChapter(chapters[0] || ""); setIndex(0); }, [section, chapters]);
  const active = useMemo(() => questions.filter((item) => item.section === section && item.chapter === chapter), [questions, section, chapter]);
  const current = active[index]; const answered = Object.keys(answers).filter((id) => active.some((item) => item.id === id)).length; const correct = active.filter((item) => answers[item.id] === item.correctOption).length;
  if (!user) return <div className="mx-auto max-w-xl px-4 py-20 text-center"><BookOpenCheck className="mx-auto text-brand" size={32}/><h1 className="mt-4 font-display text-2xl font-bold">Sign in to practise chapter-wise</h1><p className="mt-2 text-sm text-muted">Save your momentum with focused CAT practice.</p><Link href="/login?returnTo=%2Fpractice" className="mt-6 inline-flex rounded-full bg-brand px-5 py-2.5 text-sm font-bold text-white">Continue with Google</Link></div>;
  if (loading) return <div className="flex min-h-[60vh] items-center justify-center gap-2 text-sm text-muted"><Loader2 className="animate-spin text-brand"/> Loading practice questions…</div>;
  return <div className="mx-auto max-w-6xl px-4 py-10 sm:px-6 lg:px-8"><p className="text-xs font-bold uppercase tracking-wide text-brand-dark">Practice library</p><h1 className="mt-1 font-display text-3xl font-bold">Master one chapter at a time.</h1><p className="mt-2 text-sm text-muted">Choose a CAT area, solve at your pace, and check every explanation instantly.</p><div className="mt-7 grid gap-6 lg:grid-cols-[260px_1fr]"><aside className="rounded-2xl border border-border bg-white p-4 sm:p-5"><label className="text-xs font-bold uppercase tracking-wide text-muted">Section<select value={section} onChange={(event) => setSection(event.target.value)} className="mt-2 w-full rounded-xl border border-border bg-white px-3 py-2.5 text-sm font-semibold outline-none focus:border-brand"><option>Quant</option><option>VARC</option><option>DILR</option></select></label><p className="mt-5 text-xs font-bold uppercase tracking-wide text-muted">Chapters</p><div className="mt-2 space-y-1">{chapters.map((item) => <button key={item} onClick={() => { setChapter(item); setIndex(0); }} className={`w-full rounded-xl px-3 py-2.5 text-left text-sm font-semibold ${chapter === item ? "bg-brand text-white" : "text-muted hover:bg-brand-tint hover:text-brand-darker"}`}>{item}</button>)}{!chapters.length && <p className="rounded-xl bg-surface-muted p-3 text-xs text-muted">No published chapters yet.</p>}</div>{active.length > 0 && <div className="mt-6 rounded-xl bg-surface-muted p-3"><p className="text-xs font-bold uppercase tracking-wide text-muted">Your session</p><p className="mt-2 text-lg font-bold text-foreground">{correct}/{active.length} correct</p><p className="text-xs text-muted">{answered} of {active.length} answered</p><div className="mt-3 h-2 overflow-hidden rounded-full bg-white"><div className="h-full bg-brand" style={{ width: `${(answered / active.length) * 100}%` }}/></div></div>}</aside><main>{current ? <section className="rounded-2xl border border-border bg-white p-5 shadow-sm sm:p-7"><div className="flex flex-wrap items-center justify-between gap-3"><p className="text-sm font-bold text-brand-darker">Question {index + 1} of {active.length}</p><span className={`rounded-full px-3 py-1 text-xs font-bold ${styles[current.difficulty]}`}>{current.difficulty}</span></div><div className="mt-5 h-2 overflow-hidden rounded-full bg-surface-muted"><div className="h-full bg-brand transition-all" style={{ width: `${((index + 1) / active.length) * 100}%` }}/></div><h2 className="mt-7 whitespace-pre-wrap font-display text-xl font-bold leading-relaxed text-foreground">{current.question}</h2><div className="mt-6 space-y-3">{current.options.map((option, optionIndex) => { const letter = "ABCD"[optionIndex]; const chosen = answers[current.id]; const result = chosen ? (letter === current.correctOption ? "correct" : letter === chosen ? "wrong" : "") : ""; return <button key={letter} disabled={Boolean(chosen)} onClick={() => setAnswers((currentAnswers) => ({ ...currentAnswers, [current.id]: letter }))} className={`flex w-full items-center gap-3 rounded-xl border px-4 py-3.5 text-left text-sm font-medium transition ${result === "correct" ? "border-brand bg-brand-tint text-brand-darker" : result === "wrong" ? "border-red-200 bg-red-50 text-danger" : "border-border hover:border-brand hover:bg-brand-tint"}`}><span className="flex h-7 w-7 shrink-0 items-center justify-center rounded-full border border-current text-xs font-bold">{letter}</span>{option}{result === "correct" && <CheckCircle2 className="ml-auto" size={18}/>} {result === "wrong" && <XCircle className="ml-auto" size={18}/>}</button>; })}</div>{answers[current.id] && <div className="mt-5 rounded-xl border border-brand/20 bg-brand-tint p-4"><p className="font-bold text-brand-darker">{answers[current.id] === current.correctOption ? "Correct — well done." : `Correct answer: ${current.correctOption}`}</p>{current.explanation && <p className="mt-2 whitespace-pre-wrap text-sm leading-relaxed text-foreground">{current.explanation}</p>}</div>}<div className="mt-7 flex items-center justify-between gap-3"><button disabled={index === 0} onClick={() => setIndex((value) => value - 1)} className="inline-flex items-center gap-1 rounded-full border border-border px-4 py-2 text-sm font-bold disabled:opacity-40"><ChevronLeft size={16}/> Previous</button><div className="flex gap-1.5">{active.map((item, itemIndex) => <button key={item.id} onClick={() => setIndex(itemIndex)} className={`h-8 w-8 rounded-lg text-xs font-bold ${itemIndex === index ? "bg-brand text-white" : answers[item.id] ? "bg-brand-tint text-brand-darker" : "bg-surface-muted text-muted"}`}>{itemIndex + 1}</button>)}</div><button disabled={index === active.length - 1} onClick={() => setIndex((value) => value + 1)} className="inline-flex items-center gap-1 rounded-full bg-brand px-4 py-2 text-sm font-bold text-white disabled:opacity-40">Next <ChevronRight size={16}/></button></div></section> : <section className="rounded-2xl border border-dashed border-border bg-white p-12 text-center"><Circle className="mx-auto text-brand"/><h2 className="mt-4 font-display text-xl font-bold">No questions in this chapter yet</h2><p className="mt-2 text-sm text-muted">Your faculty can publish questions from the Admin Practice area.</p></section>}<button onClick={() => { setAnswers({}); setIndex(0); }} className="mt-4 inline-flex items-center gap-2 text-sm font-bold text-brand-darker"><RotateCcw size={15}/> Reset this chapter</button></main></div></div>;
}
